syntax = "proto3";
package proto;

//python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. ./file.proto

service FileService {
  rpc UploadFile(stream Bytes) returns (Response);
}

message Bytes {
  bytes value = 1;
}

message Response {
  int32 flag = 1;
}
